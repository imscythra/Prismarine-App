<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Prismarine.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Prismarine.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dwui="using:DevWinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Grid>
        <Grid Margin="12,0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="550"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Grid>
                    <TextBox HorizontalAlignment="Stretch" Margin="0,0,104,0" CornerRadius="8" Padding="32,10,32,10" BorderBrush="Transparent"
                             FontWeight="SemiBold" PlaceholderText="Enter a YouTube URL" TextAlignment="Center"/>
                    <Button Style="{ThemeResource AccentButtonStyle}" Padding="8" HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Spacing="8">
                            <StackPanel Spacing="8" Orientation="Horizontal">
                                <FontIcon Glyph="&#xE710;"/>
                                <TextBlock FontWeight="SemiBold" Text="Add link"/>
                            </StackPanel>
                        </StackPanel>
                    </Button>
                
                    <FontIcon Glyph="&#xE774;" FontSize="16" HorizontalAlignment="Left" Margin="10,0"/>
                </Grid>
                
            </StackPanel>
            <Button Padding="8" HorizontalAlignment="Right" Background="Transparent" Grid.Column="1">
                <FontIcon Glyph="&#xE713;" FontSize="16"/>
            </Button>
        </Grid>
        <Grid Margin="12,52,12,12" CornerRadius="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition MaxWidth="300"/>
            </Grid.ColumnDefinitions>

            <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                
            </Grid>
            <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Grid.Column="1">
                
            </Grid>
            <controls:GridSplitter Grid.Column="1"
                               Width="16"
                               HorizontalAlignment="Left"
                               ResizeBehavior="BasedOnAlignment"
                               ResizeDirection="Auto">
                <controls:GridSplitter.RenderTransform>
                    <TranslateTransform X="-7" />
                </controls:GridSplitter.RenderTransform>
            </controls:GridSplitter>

        </Grid>
    </Grid>
</Page>
